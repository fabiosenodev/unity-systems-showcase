# Interaction System

`PlayerManager` provides:
- Input binding for movement/actions
- Trigger-based discovery of nearby interactables
- Closest interactable selection
- Interaction prompt updates via event

Main file: `Scripts/Interaction/PlayerManager.cs`.
